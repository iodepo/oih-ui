
var config = require('./config/environment.js');

export const { appTitle,
               appShortTitle,
               appDescription,
               dataServiceUrl,
               mapboxAccessToken,
               basemapStyleLink,
               environment,
             } = config;
